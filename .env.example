# Vaangly - Environment Configuration
# Copy this file to .env and fill in your Supabase project values.

# SUPABASE (required for live auth and database)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
# Optional public web origin. On Vercel, leave blank to use the deployed origin.
VITE_APP_URL=
VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your_vapid_private_key_here
VAPID_EMAIL=mailto:your@email.com

# SUPABASE SETUP CHECKLIST (complete before testing email confirmation links)
# 1. Authentication > Providers > Email:
#    Enable Email provider and Confirm email.
# 2. Authentication > URL Configuration:
#    Site URL: https://vaangly.vercel.app (staging)
#    Redirect URLs: add https://vaangly.vercel.app/auth/callback
# 3. Authentication > Email Templates > Confirm signup:
#    Body: paste supabase/email_templates/email_verification.html
# 4. Authentication > SMTP Settings (for more than 3 emails/hour):
#    Configure Resend, SendGrid, AWS SES, or Postmark.
# 5. Database > Run migrations:
#    Apply all files in supabase/migrations/ in order.
#    Verify the on_auth_user_created trigger exists.
#
# If these variables are not set, Vaangly runs in offline preview mode.
# Use code 123456 for all OTP prompts.
